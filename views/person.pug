html 
    head 
        title= person.name
    body 
        h1= person.name
        h2(id="connection") Disconnected from Server. Please Refresh
        each event in events
            a(href=event.link)= event.name
            button(onClick="call(" + event.id + ")") Call
            br
        script(src="/static/livePerson.js") 